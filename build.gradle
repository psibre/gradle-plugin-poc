plugins {
    id 'groovy'
    id 'java-gradle-plugin'
}

version '0.1'

gradlePlugin {
    plugins {
        hello {
            id = 'org.m2ci.msp.hello-plugin'
            implementationClass = 'org.m2ci.msp.hello.HelloPlugin'
        }
    }
}
